package com.ghostchu.quickshop.addon.exchange.operations;

/** Administrator attestation about the external side effect of an uncertain custody transfer. */
public enum ReviewDecision {
  CONFIRM_EXTERNAL_SUCCESS,
  CONFIRM_EXTERNAL_FAILURE
}
